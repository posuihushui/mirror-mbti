# 付费双人相处指南：GPT-6 medium 实施提示词

以下保留实施合同；当前执行状态见 [实施记录](../verification/paid-pairing.md)。主规格：[paid-pairing-product.md](./paid-pairing-product.md)。

```text
请在 /Users/lake/work/mine/mirror-mbti 实施「付费双人相处指南」。

先完整阅读最新 AGENTS.md、docs/plans/paid-pairing-product.md、
docs/plans/referral-growth-implementation.md、docs/verification/share-growth.md，
以及主规格列出的实际代码和安装版本 Next.js 文档。
新规格优先于旧规格关于配对免费、必须先创建单人分享和旧入口层级的规定。

本任务只在用户要求实施时执行。先核对最新会话是否修改过收费规则：
主规格当前默认双方均须解锁各自用于本次配对的报告，单人相处说明书仍免费。
如果用户已选择仅发起者付费，先依主规格第 16 节同步整份文档与测试合同，
再按统一后的规则实施；不要在代码中混用两种收费方式，也不要自己改变商业规则。

先检查 git status 并记录基线，保留已有 .env 加载／迁移修复和其他未提交修改。
不重置、不自动提交、不部署。已实现的 P0/P1 是基线，不从零重做。

按主规格 PP-01 至 PP-06 依次完成，每步验证后更新
 docs/verification/paid-pairing.md，保存截图和日志至同名目录。
记录已完成、剩余、阻塞与真实验证状态；上下文不足时续接先读该文件。
不要在阶段边界重复询问是否继续。

必须交付：
1. 服务端按本人具体结果校验配对权益；新邀请／新加入付费门控。
   保留历史已解锁结果、历史免费已生成指南的读取与撤回权。
   旧免费邀请禁止新加入，不用批量 revokedAt 破坏旧指南。
2. 独立 resultId 邀请入口，shareId 可空；不要求先公开个人卡，不自动创建隐藏分享。
   双方支付与双方隐私同意完全分开；沿用 192bit token、服务端快照、幂等、限流。
3. compare-v2 三段内容、开口例句与实践；固定虚构示例和真实指南使用同一组件。
   无匹配率、无关系判断，均衡／跨版本／跨语言正确降级，旧快照不重算。
4. 付费前权益预览与 /pairing 介绍页；现有支付面板展示完整报告＋配对资格和费用规则。
   付费后在结果、报告、我的报告和 /my/pairing 提供直接入口。
   未支付全均衡结果不能购买；sample 仍引导免费测试。
5. 遵循最新 AGENTS：测试前只介绍功能；本人正常结果页、支付前说明双方权益与费用。
   原问卷→正常结果→必要时支付→显式同意完整闭环。
   服务端 continuation 支持支付回跳与身份恢复；多个待办不得串邀请。
   修复只保留 unlock 参数导致 compare 丢失的问题，拒绝任意 returnUrl。
   支付成功而结果权益未同步时服务端幂等校验，禁止重复购买／提前授权。
6. 新迁移、P0 与邀请独立归因、最小事件、只读报表、隐私帮助与双语文案。
   新能力路径和参数先脱敏再开放；复用 GA 与 quiz_start/purchase，不重复初始化。
   统计失败用 savepoint 隔离，不能阻止有效问卷提交。
7. 本规格动画，减少动态效果、无 JS、打印、失败和中断状态；默认最终态完整。
   CTA/dock 不在动画祖先中，不新增动画库、分析 SDK 或远程图片服务。

重要边界：
- 不改题库、计分、旧数组 API、旧草稿、sample/paid 结构、支付供应商验证和订单恢复授权。
- 必要的权益同步修复只复用服务端已确认 paid 的同 owner/result 订单；前端不能授权。
- 邀请公开 DTO 不含原 resultId、订单号、分数、答案或未同意内容；不是 CSS 隐藏。
- 配对内容只允许参与的两个签名 visitor 读取；任意付费用户不等于拥有任意指南。
- 可选 share→invitation→pair 的事务锁序保持一致，锁后重检权限和撤回。
- 独立邀请归因不得依赖不存在的父分享，所有 innerJoin(resultShares) 必须审查。
- 不把复制／下载／微信 SDK 配置成功写成真实分享成功。
- 不增加注册、返现、邀请任务、群组榜单、双人内容公开 PNG、匹配率或 AI 在线分析。
- 微信可选能力未核验时保持默认关闭，真实设备未验证如实记录。

验证：
完成主规格第 13 节的商业流程、权限矩阵、并发、迁移、续接与兼容验收。
运行必要 typecheck、lint、unit、真实测试 DB integration、build、e2e。
数据库仅使用确认的本地／测试环境；核对实际运行服务使用的库迁移齐全。
覆盖双付费四组合、跨结果借权、直接 API 绕过、旧免费资源、全均衡历史已购、
paid 但权益未同步、邀请支付中失效、存储失败、统计 SQL 错误和跨语言回跳。
截图至少 393×852、1363×936，中英主要状态；补 320px、720/721px、390×749。
回归旧 P0 PNG、二维码、支付和恢复，不伪报未运行检查。

可并行委派纯内容／翻译／独立测试，避免多代理同时改 schema、comparisons、
quiz、支付续接与全局样式。完成后补充实际落定的 AGENTS.md 规则。

最终简述已完成的用户流程、实际付费规则、验证结果、证据和环境限制。
不要把文档完成、复制成功或本地验证描述成已部署上线。
```
