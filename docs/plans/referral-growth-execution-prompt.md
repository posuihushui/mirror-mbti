# 给 GPT-6 medium 的执行提示词

## P0：首期实施（直接复制）

```text
请在 /Users/lake/work/mine/mirror-mbti 实施已经确定的分享传播方案 P0，含动画。

先完整阅读：
1. 最新 AGENTS.md。
2. docs/plans/referral-growth-implementation.md。
3. 该规格第0.2节列出的当前版本Next.js文档和相关代码。

用户已经认可产品方向。按规格执行，不重新提泛化裂变建议，不只输出计划，不擅自扩展到返现、注册、邀请任务或P1双人功能。

完成P0-01到P0-06：
- 三选三的相处说明书，参考类型/四维定性标签可选择公开。
- 独立公开快照、192bit随机分享token、owner-only管理、幂等创建、撤销与限流。
- 中英文结果页入口、响应式编辑预览、PNG保存/复制、好友页和我的分享。
- 7日首次触达及首次测试完成归因、最小事件、只读聚合脚本。
- 第7节全部P0动画，包括快速操作、关闭、失败、减少动态效果、打印和无JS降级。
- 迁移、字体、metadata/robots、测试与截图证据。

先检查git status，保存基线，保留已有品牌/英文logo及正在新增的GA4统计等未提交修改。只修改本任务需要的内容，不重置、不自动提交或部署。

按规格第11节逐步实现；每步验证后更新docs/verification/share-growth.md，再继续下一步。不要因为阶段边界反复询问是否继续，完成整个P0才交付。上下文不足时用该文件记录已完成、下一步、阻塞与验证状态，续接时先读它，避免重复工作。

特别重要：
- 新分享不能泄漏原resultId/未公开profile/订单号；不能只是CSS隐藏。
- 候选和公开快照由服务端根据本人结果生成；客户端数据不能授权。
- snapshot关闭字段必须缺省，含RSC/HTML/OG/图片均不可泄漏。
- 不改题库、计分、旧草稿、sample/paid架构、支付与恢复权限。
- 不把复制/下载/微信SDK配置成功称为真实分享成功。
- 统计失败不能阻止有效测试提交；原数组API继续兼容。
- 既有analytics/url.ts尚需覆盖新token路由，先做脱敏再开放新页面；复用已有GA初始化和quiz_start，不重复埋点，推荐归因以自有DB为准。
- 事务内归因失败使用savepoint隔离，不能吞掉SQL错误却使results主事务一起失败。
- 默认最终态完整，动画只增强；CTA/dock不在动画祖先里。
- 不新增动画库、第三方分析SDK、html2canvas或远程图片生成服务。
- 微信可选配置未核验时保持默认关闭，通用保存和复制正常交付。

使用仓库已有技术；可并行委派纯内容/独立测试等有清晰文件边界的子任务，避免多代理同时修改schema、quiz、results和全局样式。

执行必要typecheck、lint、unit、build、e2e；数据库只用确定的本地/测试环境。截图检查393×852、1363×936，补320px与断点；实际检查生成PNG及二维码。不能运行的检查明确记录，不伪报通过。

最终简述：完成内容、验证结果、证据文件、环境限制。P1是后续单独任务，不要把P0完成描述成双人功能已经上线。
```

## P1：第二期实施（P0验收后复制）

```text
在 /Users/lake/work/mine/mirror-mbti 继续实施双人相处提示 P1，含动画。

先读最新AGENTS.md、docs/plans/referral-growth-implementation.md和docs/verification/share-growth.md，检查P0实际完成状态与当前工作树。以现有代码为基线，不重做P0，不覆盖其他修改。

执行规格第6节、第7节P1动画、第11节P1任务及第12节对应验收：
- A单独确认公开四维定性类别并创建邀请，P0分享开关不被暗中扩权。
- B选择本人已有结果或在现有问卷完成测试；先看自己正常结果，再明确同意对照。
- 邀请页公开A同意的内容，双人页仅双方签名visitor可读。
- 根据双方同意的定性类别生成并冻结三段沟通提示，无匹配率、无分数比较。
- 任意一方可撤回；share/邀请撤销与加入事务并发安全；邀请过期只阻止新加入。
- 双语、跨版本、全均衡、无共同点/无差异、存储失败、邀请失效都按规格降级。
- 加入有限的双方面板汇合和提示依次出现动画，复用现有动画机制。

全过程不修改用户原始测试和已购报告，不新增注册、现金奖励、公开成员榜或付费双人产品。保持P0和既有支付/恢复/sample流程通过测试。

分步完成并更新验证记录，运行全部必要检查、补齐截图与权限/并发/动画测试；明确真实微信未验证项。不自动部署。最终给出完整交付和验证结果。
```
